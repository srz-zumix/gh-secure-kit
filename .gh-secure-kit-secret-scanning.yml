# Local secret scanning configuration for gh-secure-kit itself.
# Used by: gh secure-kit secret-scanning local check
allowlist:
  paths:
    # Test fixtures contain structurally valid but fake secrets.
    - internal/localscan/*_test.go
